# Test Fix Orchestrator Dependencies
# Install with: pip install -r test_orchestrator_requirements.txt
# or with uv: uv pip install -r test_orchestrator_requirements.txt

# Claude Agent SDK
claude-agent-sdk>=0.1.0

# Rich terminal UI
rich>=13.0.0

# Environment variables
python-dotenv>=1.0.0

# Async support (usually built-in but just in case)
nest-asyncio>=1.5.0
