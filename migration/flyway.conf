# Flyway Configuration
# Used by the migration container

# General settings
flyway.mixed=true
flyway.validateOnMigrate=true
flyway.cleanDisabled=true
flyway.outOfOrder=false

# Table to track migrations (different from our custom archon_migrations)
flyway.table=flyway_schema_history

# Encoding
flyway.encoding=UTF-8

# Placeholders for environment-specific values
flyway.placeholders.archon_version=0.1.0

# SQL migration settings
flyway.sqlMigrationPrefix=V
flyway.sqlMigrationSeparator=__
flyway.sqlMigrationSuffixes=.sql

# Baseline (for existing databases)
flyway.baselineVersion=0
flyway.baselineDescription=Initial baseline
