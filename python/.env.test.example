# Test Environment Configuration
# Copy this file to .env.test and fill in the values for running tests

# Supabase Configuration (Required for integration tests)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-key-here

# OpenAI Configuration (Optional - only for tests that use embeddings/LLMs)
OPENAI_API_KEY=sk-your-api-key-here

# Logfire Configuration (Optional - usually disabled for tests)
LOGFIRE_TOKEN=your-logfire-token
LOGFIRE_ENABLED=false

# Test-specific settings
PYTEST_TIMEOUT=300
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1

# Optional: Use local test database instead of cloud
# TEST_DATABASE_URL=postgresql://postgres:postgres@test-db:5432/postgres

# Optional: Test with specific models
# DEFAULT_EMBEDDING_MODEL=text-embedding-3-small
# DEFAULT_LLM_MODEL=gpt-4o-mini
